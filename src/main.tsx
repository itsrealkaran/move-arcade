import ReactDOM from "react-dom/client";
import "./index.css";
// import App from "./app.tsx";
import App from "./stack.tsx";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />
);
